// Paddings
import 'package:flutter/material.dart';

const kPadding = 8.0;
const kButtonRadius = kPadding * 2;
const kButtonHeight = kPadding * 7;
const kBannerHeight = kPadding * 37.5;
const kCircularRadius = Radius.circular(kButtonRadius);
